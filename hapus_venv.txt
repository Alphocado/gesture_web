deactivate
Remove-Item -Recurse -Force gesture_env
python -m venv gesture_env

kalau powershell
gesture_env\Scripts\activate

kalau gitbash
source gesture_env/Scripts/activate

python -m pip install --upgrade pip
pip install -r requirements.txt
